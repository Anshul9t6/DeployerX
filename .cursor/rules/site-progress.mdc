---
description: Keep docs/data/progress.json in sync with coverage sources
globs: locale-packs/**/*,playbooks/**/*,field-notes/**/*,docs/**/*,scripts/generate_site_data.py
alwaysApply: false
---

# Site data sync

After changing locale packs, playbooks, field notes, or the generator:

```bash
make site && make check
```

Include `docs/data/progress.json` in the same change set. Do not hardcode coverage counts in HTML.
